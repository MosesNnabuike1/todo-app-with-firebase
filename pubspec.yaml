name: todoapp_improved
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.11.0
  firebase_auth: ^5.4.2
  shared_preferences: ^2.0.13
  provider: ^6.1.2
  cupertino_icons: ^1.0.6
  cloud_firestore: ^5.6.3
  firebase_ui_auth: ^1.16.1
  sqflite: ^2.4.1
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
